# Edge CV Monitor — Python dependencies
# For Jetson, install opencv-python-headless (GPU-accelerated via JetPack)
# For x86, standard opencv-python works fine.

opencv-python-headless>=4.8.0
numpy>=1.24.0

# Roboflow inference SDK (comment out for demo mode)
# inference>=0.9.0
# roboflow>=1.1.0

# Networking / telemetry
requests>=2.31.0

# Optional: structured logging
# python-json-logger>=2.0.7
